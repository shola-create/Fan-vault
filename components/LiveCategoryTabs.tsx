"use client";
import { useMemo, useState } from "react";
import Link from "next/link";
type Room={id:string;roomName:string;title:string;category:"NORMAL"|"MATURE"|"DARE";creator:{username:string;displayName:string;avatarUrl?:string|null}};
const labels={NORMAL:"Normal",MATURE:"18+ Mature",DARE:"Dare"};
export default function LiveCategoryTabs({rooms}:{rooms:Room[]}){const [tab,setTab]=useState<Room["category"]>("NORMAL");const shown=useMemo(()=>rooms.filter(r=>r.category===tab),[rooms,tab]);return <div><div className="mb-5 flex gap-2 overflow-x-auto">{(Object.keys(labels) as Room["category"][]).map(x=><button key={x} onClick={()=>setTab(x)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm ${tab===x?"bg-stone-900 text-white":"border border-stone-300 text-stone-600"}`}>{labels[x]}{x==="MATURE"&&" 🔞"}{x==="DARE"&&" 🎯"}</button>)}</div>{shown.length===0?<div className="rounded-2xl border border-stone-200 bg-white p-8 text-center text-stone-500">No {labels[tab].toLowerCase()} lives right now.</div>:<div className="grid gap-4 sm:grid-cols-2">{shown.map(r=><Link key={r.id} href={`/live/${r.roomName}`} className="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm hover:shadow"><p className="text-xs font-medium text-red-600">● LIVE · {labels[r.category]}</p><h2 className="mt-2 text-lg font-semibold">{r.title}</h2><p className="text-sm text-stone-500">{r.creator.displayName} · @{r.creator.username}</p></Link>)}</div>}</div>}
