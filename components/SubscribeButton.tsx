"use client";
import { useState } from "react";
export default function SubscribeButton({username,monthlyPriceCents}:{username:string;monthlyPriceCents:number}){const[loading,setLoading]=useState(false);async function handle(){setLoading(true);const r=await fetch('/api/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({creatorUsername:username})});if(r.status===401){location.href='/login';return;}const d=await r.json();if(d.url)location.href=d.url;else{setLoading(false);alert(d.error||'Could not start checkout')}}return <button onClick={handle} disabled={loading} className="shrink-0 rounded-full bg-[--accent] px-5 py-2 text-white disabled:opacity-50">{loading?'Redirecting…':`Subscribe · ₦${(monthlyPriceCents/100).toLocaleString()}/mo`}</button>}
