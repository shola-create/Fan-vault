import Link from "next/link";
import { prisma } from "@/lib/prisma";
import LiveCategoryTabs from "@/components/LiveCategoryTabs";
export default async function LiveIndexPage(){const rooms=await prisma.liveRoom.findMany({where:{status:"LIVE"},include:{creator:true},orderBy:{startedAt:"desc"}});const safeRooms=rooms.map(r=>({id:r.id,roomName:r.roomName,title:r.title,category:r.category as "NORMAL"|"MATURE"|"DARE",creator:{username:r.creator.username,displayName:r.creator.displayName,avatarUrl:r.creator.avatarUrl}}));return <main className="mx-auto max-w-4xl px-6 py-10"><div className="mb-8 flex items-center justify-between"><div><h1 className="text-3xl font-semibold">🔴 Live</h1><p className="text-stone-500">Choose a live section. Live viewing requires the ₦3,700/month FAN pass or an active subscription to the creator.</p></div><Link href="/dashboard" className="rounded-full border border-stone-300 px-4 py-2 text-sm">Dashboard</Link></div><div className="mb-6 rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900">The 18+ Mature section is age-gated and subject to your platform rules and local law. Dare prompts must stay within your community rules and cannot be used for illegal or non-consensual activity.</div><LiveCategoryTabs rooms={safeRooms}/></main>}
